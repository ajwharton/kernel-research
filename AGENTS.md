# Agent rules (auto-loaded)

**Session entry:** read **`start.md`** — full thin card for kernel-research.

When the user says `start`, `read start.md`, or opens a fresh session: **read `start.md` once**, then act. Do not preload all of `learnings/` or `results/`.

Thin harness: `~/.grok/docs/thin-harness.md`. Agent memory: Qdrant `agent-memory__*` on miss only.
